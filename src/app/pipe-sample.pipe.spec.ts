import { PipeSamplePipe } from './pipe-sample.pipe';

describe('PipeSamplePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSamplePipe();
    expect(pipe).toBeTruthy();
  });
});
